# ========================================
# .gitignore cho dự án Next.js + Supabase
# Tạo bởi: [tên bạn]
# Ngày: [ngày tạo]
# ========================================

# --- Dependencies ---
# Thư mục chứa mã nguồn thư viện, rất nặng, không cần push
node_modules/

# --- Environment Variables (BÍ MẬT!) ---
# QUAN TRỌNG: KHÔNG BAO GIỜ push file .env lên GitHub
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Next.js Build Output ---
# File được tạo khi chạy `npm run build`, không cần lưu
.next/
out/

# --- Debug Logs ---
# File log sinh ra khi có lỗi, không cần theo dõi
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- OS Files ---
# File ẩn của hệ điều hành, không liên quan đến dự án
.DS_Store
Thumbs.db
*.swp
*.swo

# --- IDE/Editor ---
# Cấu hình riêng của từng người, không nên chia sẻ
.vscode/
!.vscode/extensions.json
.idea/
*.sublime-project
*.sublime-workspace

# --- TypeScript ---
# File được tạo từ quá trình build TypeScript
*.tsbuildinfo
next-env.d.ts

# --- Vercel ---
# Cấu hình local của Vercel CLI
.vercel

# --- Supabase ---
# Nếu dùng Supabase CLI local
supabase/.branches
supabase/.temp

# --- Testing ---
# Kết quả test và coverage report
coverage/
playwright-report/
test-results/

# --- Miscellaneous ---
# File tạm, backup, và những thứ không cần thiết
*.log
*.tmp
*.bak
*.cache
.turbo
